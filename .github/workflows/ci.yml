name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

env:
  MONGODB_URI: mongodb://127.0.0.1:27017/ananttatt-ci

jobs:
  quality:
    name: Lint, test, and build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
      - name: Install pnpm
        uses: pnpm/action-setup@v4
      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Run ESLint
        run: pnpm lint
      - name: Run unit and API tests
        run: pnpm test
      - name: Build application
        run: pnpm build
      - name: Check high-severity dependencies
        run: pnpm audit --audit-level=high

  browser-test:
    name: Report-flow browser test
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      MONGODB_URI: mongodb://127.0.0.1:27017/ananttatt-ci
      NEXT_PUBLIC_FIREBASE_API_KEY: e2e-test-api-key
      NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: e2e-test.firebaseapp.com
      NEXT_PUBLIC_FIREBASE_PROJECT_ID: e2e-test-project
      NEXT_PUBLIC_FIREBASE_APP_ID: 1:123456789:web:e2etest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
      - name: Install pnpm
        uses: pnpm/action-setup@v4
      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Install Chromium
        run: pnpm exec playwright install --with-deps chromium
      - name: Build production test server
        run: pnpm build
      - name: Run report-flow test
        run: pnpm test:e2e
      - name: Upload Playwright report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v5
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 14
