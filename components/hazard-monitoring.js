import { Activity, AlertTriangle, Clock3, ShieldCheck } from "lucide-react";
import { HazardMap } from "@/components/HazardMap";

export function HazardMonitoring({ hazards, loading, error }) {
    const highRisk = hazards.filter((hazard) => hazard.severity === "high").length;
    const emergencies = hazards.filter((hazard) => hazard.emergency).length;
    return <section className="border-y border-blue-100 bg-white py-8 lg:py-10"><div className="mx-auto max-w-[1600px] px-5 lg:px-8"><div className="rounded-xl border border-blue-200 bg-white p-4 shadow-sm sm:p-5"><div className="mb-4 flex flex-wrap items-start justify-between gap-3"><div><div className="flex items-center gap-2"><h2 className="text-2xl font-bold text-slate-900">India hazard monitoring grid</h2><LiveBadge /></div><p className="mt-1 text-sm text-slate-600">Published community reports update automatically.</p></div></div>{error && <p className="mb-3 rounded-lg border border-red-200 bg-red-50 px-3 py-2 text-sm text-red-800">{error}</p>}{loading ? <MapLoading /> : <HazardMap hazards={hazards} className="h-[530px] w-full rounded-lg" />}<div className="mt-3 flex items-center gap-2 text-xs text-slate-600"><Clock3 className="h-3.5 w-3.5 text-blue-600" />Locations are approximate to protect privacy. Report only when it is safe.</div><div className="mt-4 grid grid-cols-3 gap-px overflow-hidden rounded-lg border border-blue-100 bg-blue-100"><Stat label="Active reports" value={loading ? "—" : hazards.length} icon={Activity} tone="text-blue-600" /><Stat label="High risk" value={loading ? "—" : highRisk} icon={AlertTriangle} tone="text-amber-600" /><Stat label="Emergency flags" value={loading ? "—" : emergencies} icon={ShieldCheck} tone="text-red-600" /></div></div></div></section>;
}
function LiveBadge() { return <div className="flex items-center gap-1.5 rounded-full bg-emerald-50 px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider text-emerald-700"><span className="h-1.5 w-1.5 rounded-full bg-emerald-500" /> Live</div>; }
function MapLoading() { return <div className="h-[530px] w-full animate-pulse rounded-lg bg-blue-50" />; }
function Stat({ label, value, icon: Icon, tone }) { return <div className="bg-white p-4 sm:p-5"><Icon className={`mb-3 h-4 w-4 ${tone}`} /><p className="text-2xl font-bold text-slate-900 sm:text-3xl">{value}</p><p className="mt-1 text-xs font-semibold text-slate-600">{label}</p></div>; }
