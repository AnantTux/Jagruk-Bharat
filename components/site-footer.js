import { Github, Linkedin, Mail, ShieldCheck } from "lucide-react";
import { SITE } from "@/lib/site";

export function SiteFooter({ compact = false }) {
    return <footer className="border-t border-border bg-white text-slate-600"><div className="mx-auto flex max-w-[1440px] flex-col gap-6 px-4 py-8 sm:px-6 md:flex-row md:items-start md:justify-between lg:px-8"><div className="max-w-xl space-y-3"><div className="flex items-center gap-2"><ShieldCheck className="h-5 w-5 text-primary" /><span className="text-lg font-bold text-slate-950">{SITE.name}</span><span className="text-sm text-primary">· {SITE.tagline}</span></div>{!compact && <p className="text-sm leading-5">{SITE.about}</p>}<p className="text-xs text-slate-500">© {new Date().getFullYear()} {SITE.name}. Built by Anant.</p></div><div className="space-y-3 md:text-right"><p className="text-xs font-semibold uppercase tracking-[0.12em] text-slate-500">Connect</p><div className="flex flex-wrap gap-4 text-sm md:justify-end"><a href={`mailto:${SITE.email}`} className="inline-flex items-center gap-2 hover:text-primary"><Mail className="h-4 w-4" />Email</a><a href={SITE.linkedIn} target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-2 hover:text-primary"><Linkedin className="h-4 w-4" />LinkedIn</a><a href={SITE.github} target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-2 hover:text-primary"><Github className="h-4 w-4" />GitHub</a></div></div></div></footer>;
}
